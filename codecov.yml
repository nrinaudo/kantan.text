comment:
  layout: header, changes, diff
coverage:
  notify:
    gitter:
      default:
        branches:
        - master
        threshold: 0
        url: https://webhooks.gitter.im/e/f18fd5772bea646db070
  status:
    patch: false
